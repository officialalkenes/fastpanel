# FastPanel Example — environment variable reference
# Copy to .env and adjust as needed.

# Enable FastPanel (set to false in production!)
FASTPANEL_ENABLED=true

# SQL query slow threshold in milliseconds
FASTPANEL_SLOW_QUERY_MS=50.0

# Maximum number of requests to keep in the store
FASTPANEL_STORE_MAX_REQUESTS=100
